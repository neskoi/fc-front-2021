import styled from 'styled-components'

const Header = styled.h2`
  width: 90%;
  border-radius: 32px;
  margin: 32px 0;
  padding: 8px 0;
  font-family: Roboto;
  font-weight: 400;
  font-size: 18px;
  background: var(--blue);
  color: var(--white);
`

const Styled = {
  Header,
}

export default Styled